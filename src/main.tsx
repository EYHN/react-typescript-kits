import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { App } from './App';

const Main = () => (
    <App />
);

ReactDOM.render(
  <Main />,
  document.getElementById('app')
);
